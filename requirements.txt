# Pinned runtime set.
#
# `pyproject.toml` is the source of truth for dependency *ranges*. This file is
# the pinned set used by `start.sh` and the release workflow, where a
# reproducible bundle matters more than flexibility.
#
# For development prefer:  pip install -e ".[providers,dev]"
# Tooling only:            pip install -r requirements-dev.txt
#
# Removed in Phase 0: `asyncio==3.4.3` (asyncio is in the standard library; the
# PyPI package of that name is a 2015 backport that shadows it), plus duplicate
# pins for rich, click and typer.

# Agent vocabulary and provider adapters — see docs/adr/0001-adopt-datapizza-ai.md
datapizza-ai-core==0.1.3

# CLI + TUI
typer==0.12.3
click==8.1.7
rich==13.7.1
textual>=0.80.0
inquirer==3.2.4

# Config + validation
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
PyYAML==6.0.1

# HTTP
httpx==0.27.0
aiohttp==3.9.5
websockets==12.0

# Remote inference providers (optional at runtime; pinned here for bundles)
openai==1.30.1
anthropic==0.25.7
google-generativeai==0.5.4

# Local API server (brain + sync UI)
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Document parsing
pdfplumber>=0.11.0
pypdf>=4.0.0
python-docx>=1.1.0
openpyxl>=3.1.0

# File & system
watchdog==4.0.0
psutil==5.9.8

# Auth & security
PyJWT==2.8.0
cryptography==42.0.7

# Logging
loguru==0.7.2
