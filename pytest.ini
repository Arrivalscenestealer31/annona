[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --cov=runner --cov-report=html --cov-report=term-missing
markers =
    unit: pure unit tests, no I/O
    integration: tests that touch real filesystem or subprocess
